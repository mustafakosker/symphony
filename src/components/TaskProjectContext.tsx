import { useEffect,useRef,useState } from 'react';import type { ConnectedTask } from '../../shared/contracts';import type { SourcePreview as Source } from '../../shared/projects';import type { ProjectApi } from '../projects/api';import { SourceReportView } from './SourceReport';import SourcePreview from './SourcePreview';
export default function TaskProjectContext({task,api}:{task:ConnectedTask;api:ProjectApi}){const [source,setSource]=useState<Source|null>(null),[error,setError]=useState(''),request=useRef<AbortController|null>(null);useEffect(()=>()=>request.current?.abort(),[task.id]);const context=task.projectContext;
 return <section className="task-project-context" aria-label="Pinned project context"><h3>Pinned project context</h3>{context.targetId===null&&<p>Reference-only investigation · no future change target</p>}{context.projects.map(p=><details key={p.projectId}><summary><strong>{p.name}</strong> · {context.targetId===p.projectId?'Future change target':'Reference'} · Read-only</summary><p>Task source: {p.ref} · <code>{p.snapshot.commit}</code></p>{p.brief&&<><p>Brief v{p.brief.version} · {p.brief.author} · source <code>{p.brief.source.commit.slice(0,12)}</code></p><SourceReportView report={p.brief.report} onCitation={id=>{request.current?.abort();const c=new AbortController();request.current=c;setError('');api.briefCitation(p.projectId,p.brief!.version,id,c.signal).then(v=>{if(!c.signal.aborted)setSource(v);}).catch(e=>{if(!c.signal.aborted)setError(e instanceof Error?e.message:'Source unavailable');});}}/></>}</details>)}{error&&<p role="alert">Source unavailable: {error}</p>}<SourcePreview source={source} onClose={()=>setSource(null)}/></section>;
}
