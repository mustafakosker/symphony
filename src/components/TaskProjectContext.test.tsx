// @vitest-environment jsdom
import '@testing-library/jest-dom/vitest';import { expect,it } from 'vitest';import { render,screen } from '@testing-library/react';import { draftTask } from '../../server/testing/fixtures';import { projectContextFixture } from '../../server/testing/projects';import TaskProjectContext from './TaskProjectContext';
it('shows immutable names and distinguishes task and brief commits',()=>{const context=projectContextFixture();context.projects[0].name='Saved project';context.projects[0].brief!.source.commit='f'.repeat(40);render(<TaskProjectContext task={{...draftTask(),schemaVersion:2,purpose:'task',projectId:null,projectContext:context}} api={{} as never}/>);expect(screen.getByText('Saved project')).toBeVisible();expect(screen.getByText(/Future change target/)).toBeVisible();expect(screen.getByText(/Brief v1/)).toHaveTextContent('ffffffffffff');expect(screen.getByText(/Read-only/)).toBeVisible();});
