// @vitest-environment jsdom
import '@testing-library/jest-dom/vitest';import { expect,it,vi } from 'vitest';import { render,screen,fireEvent } from '@testing-library/react';import { SourceReportView } from './SourceReport';
it('makes only registered citation markers interactive and escapes report text',()=>{const click=vi.fn();render(<SourceReportView report={{format:'source-report-v1',text:'Validation [cite:check]. <script>alert(1)</script> [cite:unknown]',citations:[{id:'check'} as never]}} onCitation={click}/>);fireEvent.click(screen.getByRole('button',{name:'Source check'}));expect(click).toHaveBeenCalledWith('check');expect(document.querySelector('script')).toBeNull();expect(screen.getByText(/<script>alert/)).toBeVisible();expect(screen.queryByRole('button',{name:'Source unknown'})).toBeNull();});
