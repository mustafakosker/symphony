// @vitest-environment jsdom
import "@testing-library/jest-dom/vitest";
import { expect,it,vi } from 'vitest';import { render,screen,fireEvent,waitFor } from '@testing-library/react';import ProjectSettings from './ProjectSettings';import type { ProjectApi } from '../projects/api';
it('saves an explicit root and preserves edited text after a failed save',async()=>{const api={settings:vi.fn().mockResolvedValue({projectsRoot:null,revision:'r1',generation:'unset',state:'unset',message:null}),saveSettings:vi.fn().mockRejectedValue(new Error('Changed elsewhere')),rescan:vi.fn()} as unknown as ProjectApi;render(<ProjectSettings api={api}/>);const field=await screen.findByLabelText('Projects root folder');fireEvent.change(field,{target:{value:'/srv/investigation-projects'}});fireEvent.click(screen.getByText('Save projects root'));await waitFor(()=>expect(api.saveSettings).toHaveBeenCalledWith(expect.objectContaining({projectsRoot:'/srv/investigation-projects',expectedRevision:'r1'})));expect(await screen.findByRole('alert')).toHaveTextContent('Changed elsewhere');expect(field).toHaveValue('/srv/investigation-projects');expect(screen.getByText(/externally maintained/i)).toBeVisible();});
