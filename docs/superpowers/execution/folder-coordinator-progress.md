# SDD ledger — plan: docs/superpowers/plans/2026-09-21-folder-coordinator.md

User selected subagent execution; plan approved.
Ruling: Work directly in existing non-Git workspace — approved plan explicitly requires it — no Git history; snapshots and reports retained in .superpowers/folder-coordinator.
Baseline: 24/24 tests pass; npm run build passes.
Ruling: Use local snapshot/diff equivalents instead of Git-dependent SDD scripts — Git absent — retain artifacts rather than deleting review record.

## Preflight
| Tasks | Shared requirement / check | Result |
|---|---|---|
| 1 self | Files, tests, deliverable and acceptance compared | Consistent with spec; code snippets are illustrative, not a complete implementation. |
| 2 self | Files, tests, deliverable and acceptance compared | Consistent with spec; code snippets are illustrative, not a complete implementation. |
| 3 self | Files, tests, deliverable and acceptance compared | Consistent with spec; code snippets are illustrative, not a complete implementation. |
| 4 self | Files, tests, deliverable and acceptance compared | Consistent with spec; code snippets are illustrative, not a complete implementation. |
| 5 self | Files, tests, deliverable and acceptance compared | Consistent with spec; code snippets are illustrative, not a complete implementation. |
| 6 self | Files, tests, deliverable and acceptance compared | Consistent with spec; code snippets are illustrative, not a complete implementation. |
| 7 self | Files, tests, deliverable and acceptance compared | Consistent with spec; code snippets are illustrative, not a complete implementation. |
| 8 self | Files, tests, deliverable and acceptance compared | Consistent with spec; code snippets are illustrative, not a complete implementation. |
| 9 self | Files, tests, deliverable and acceptance compared | Consistent with spec; code snippets are illustrative, not a complete implementation. |
| 10 self | Files, tests, deliverable and acceptance compared | Consistent with spec; code snippets are illustrative, not a complete implementation. |
| 11 self | Files, tests, deliverable and acceptance compared | Consistent with spec; code snippets are illustrative, not a complete implementation. |
| 1 → 2 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 1 → 3 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 1 → 4 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 1 → 5 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 1 → 6 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 1 → 7 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 1 → 8 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 1 → 9 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 1 → 10 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 1 → 11 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 2 → 3 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 2 → 4 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 2 → 5 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 2 → 6 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 2 → 7 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 2 → 8 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 2 → 9 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 2 → 10 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 2 → 11 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 3 → 4 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 3 → 5 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 3 → 6 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 3 → 7 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 3 → 8 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 3 → 9 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 3 → 10 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 3 → 11 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 4 → 5 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 4 → 6 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 4 → 7 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 4 → 8 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 4 → 9 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 4 → 10 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 4 → 11 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 5 → 6 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 5 → 7 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 5 → 8 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 5 → 9 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 5 → 10 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 5 → 11 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 6 → 7 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 6 → 8 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 6 → 9 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 6 → 10 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 6 → 11 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 7 → 8 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 7 → 9 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 7 → 10 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 7 → 11 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 8 → 9 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 8 → 10 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 8 → 11 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 9 → 10 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 9 → 11 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |
| 10 → 11 | Shared Task/Workflow/decision contracts and sequential dependency | Use exported source contracts; changes require controller ruling and matching consumer updates. |

Ruling: Shared contract may gain necessary fields where prose mandates persisted data absent from its illustrative type — spec governs — avoid silently omitting requirements.
Ruling: Safe role capability configuration is required before real execution; tests use fake CLI and disposable roots — no real company work is launched as part of tests.

Task 1: in progress.

Ruling: Exclude .superpowers/** and dist-server/** from Vitest discovery — snapshot test copies inflated first full-suite count — task 1 implementer correcting and rerunning before review.

Task 1: fix round 1/5 — review found human-target approval dead end and first-step insertion failing parse round-trip.
Ruling: Sequential allowsStepId points to immediate next step, null only at end; consecutive human checkpoints create next pending review. First-step insertion with $triage producer is supported — preserves approved plan.
Ruling: Task 6 owns retry-count derivation across attempts; it must prove at most two automatic retries.

Task 1: complete — review fixes cleared by independent scoped reviewer; 53/53 tests, UI/server builds passing.
Task 2: in progress.

Task 2: fix round 1/5 — independent review found concurrent stale-lock reclamation and unresumable pre-event task creation.
Ruling: Serialize stale-owner reclamation with a separate exclusive recovery lock, conservatively fail when recovery ownership uncertain; partial creation must retain a matching creation intent and resume without accepting arbitrary unrelated directories.

Task 2: fix round 2/5 — scoped review identified crash leaving only recognized atomic temp files in creation staging. Ruling: recover temp-only unpublished staging without permanent degraded issue; preserve conflicts for unknown bytes/symlinks.

Task 2: complete — all review findings cleared; final focused 26/26 store tests and build pass; prior full 77/77 pass.
Task 3: in progress.

Task 3: fix round 1/5 — preserve changed source during cleanup race, persist conflict issues across restart, and read submissions through no-follow handles. Ruling: atomic rename to recoverable private holding area before any deletion; reread held bytes, archive mismatch, preserve later source version.

Task 3: fix round 2/5 — holding path still vulnerable to pre-opened writer before unlink; oversized replacement lacked task-bound conflict.
Ruling: retain claimed source inode under .intake/received, no automatic deletion/retention cleanup in v1; periodically compare and persist task-bound conflict for later mutation. Preserves submitted content at cost of disk growth. Oversized held content remains archived with explicit conflict without unbounded read.

Task 3: complete — scoped review cleared retention/oversize fixes; 38 focused tests/build pass and independent 3 regression tests pass.
Deferred final-review item: intake readRegular checks size then handle.readFile; concurrent growth can exceed allocation bound. Task 11 must replace with bounded max+1 read and regression before final completion.
Task 4: in progress.

Task 4: fix round 1/5 — allow task-produced commits/dirty work during owned checkout reuse with base ancestry; branch names include task and repository ID to avoid source alias collision. Also fix latest-tag noncommit handling and exact origin URL validation.

Task 4: complete — all 4 findings fixed/reviewed; 111/111 tests and builds pass. No pre-fix real worktrees exist, so migration note is inapplicable.
Task 5: in progress.

Ruling: Task5 adds verifiedProfilesPath:null default, unsynced operator attestation matching CLI version/profile/actions/sandbox and covered config-policy hashes. Real launch fails closed without matching deployment verification. Fake injected runner remains test-only. Attestation documents operator trust, not automatic proof of all ambient credentials.

Ruling: Assignment includes staged material text, not just names/inaccessible file refs; role.skills initially absolute approved SKILL.md paths covered by deployment hashes. Task6 owns bounded staging; Task5 owns prompt and capability checks.

Cross-task finding: Task4 registry chose workspace/config/project.json and config/roles.json, inconsistent with approved projects/ and roles/ layout. Task6 must move loader/attestation expectations and fixtures to projects/projects.json and roles/roles.json; examples retain document shapes.

Task 5: review/fix round1 — report-ready message preceded final freeze; implementer continued process-group race fix. Reviewer found duplicate capability sets, delayed log-write rejection handling, and child-controlled log symlink writes. Implementer addressing together; regenerate final diff before scoped review. Previous 127 tests is pre-fix evidence, not final.

Task 5: fix round 2/5 — delayed log failure during close writeChain drain can initiate termination after first await; completion must await newly established termination before settling.

Task 5: complete — all original and introduced review findings cleared; final focused adapter17/17+server build pass, prior full131/131+fullbuild pass, independent final regression pass.
Task 6: in progress.
Task 6: implementation verified, review pending — coordinator/recovery and approved registry path alignment implemented; focused controlled-runner/temporary-store races pass; final full suite 153/153 and full UI/server build pass. No real Codex/company repository work or Git commands.

Task 6: fix round 1/5 — release settled slot and complete stop/shutdown after failed process registration; reject checkpoint insertion before active step, retain future insertion; align MCP prompt with evidence JSON contract.
Task 6: fix round 1 verified — both registration failure modes, active-step insertion and MCP prompt were RED→GREEN; final full suite 156/156 and UI/server build pass. Awaiting controller review.

Task 6: complete — fix review approved; full156/156+build and independent focused40/40 pass.
Task 7: in progress.

Task 7: fix round 1/5 — bind checkpoint artifacts to current producer result; block omitted replacement instead of historic fallback. Revised workflow must reopen any checkpoint whose approval was not validly carried and prevent all downstream dispatch.

Task 7: fix round 1/5 — 2 addressed; new Important stale human checkpoint marker prevents advancement after renewed approval.
Task 7: fix round 2/5 — clear accepted checkpoint staleness and prove downstream and terminal advancement.

Task 7: complete — scoped round2 review approved; 73 covering tests/server build pass after prior full188/build.
Task 8: in progress.

Task 8: fix round 1/5 — provide confined active-run logs; bound unconfirmed shutdown and persist uncertain run before releasing resources.

Task 8: fix round 1/5 — live logs resolved; shutdown partially resolved.
Task 8: fix round 2/5 — bound registration-path stop so in-flight tick cannot hang shutdown before uncertainty persistence.

Task 8: fix round 2/5 — unconfirmed stop bound addressed; registration promise itself can still stall shutdown.
Task 8: fix round 3/5 — stop/fence active slot before waiting on ticking; fence late registration/spawn continuation. Ruling: unavailable persistence cannot guarantee uncertain event; preserve existing durable launch intent and report cleanup failure rather than claim uncertainty was saved.

Task 8: complete — round3 scoped review approved; 214/214 tests and full build pass.
Task 9: in progress.

Task 9: fix round 1/5 — slow poll starvation and mutation transport disconnect. Ruling: include reviewer minor keyboard check and premature submitted copy in fix because both are explicit spec acceptance gaps, not discretionary polish.

Task 9: fix round 1/5 — original four findings addressed; new stalled superseded GET blocks interval polling.
Task 9: fix round 2/5 — retire superseded load ownership while keeping stale-response fence.

Task 9: complete — round2 scoped review approved; full215/215 and build pass.
Task 10: in progress.

Task 10: fix round 1/5 — gate checkpoint insertion during pending decision or control intent in UI and reducer. Ruling: require resolving current review before inserting another checkpoint; prevents invalidating its workflow binding; cost is one less editing option while paused. Strengthen vacuous delayed-response assertion as explicit acceptance test.
Task 10: minor (deferred to Task11/final): completed empty log streams keep polling and show waiting copy; preview bounds displayed slice but not network read.
Ruling: ArtifactRef has no MIME field; v1 artifact endpoint remains attachment/text-safe, binary distinction must use bounded strict UTF8 preview detection plus download fallback rather than adding an unplanned authoritative MIME schema.

Task 10: complete — scoped review approved; full234/234 and build pass; two minors assigned Task11.
Task 11: in progress.

Task 11: fix round 1/5 — interrupt actual final-output-before-accept window and assert side-effect count; strengthen real smoke answer-dependent evidence; add concrete capability manifest/settings/hash procedure. Controller independently verified pre-fix252 pass1skip+build.

Task 11: complete — fix re-review approved for local slice; 253 passed1skip/fullbuild.
Task 11: minor (deferred to final review): smoke summary inequality can fail even when answer-dependent evidence is valid.
Final whole-system review: in progress. All task reviews cleared; deployment prerequisites remain external and unverified.

Final whole-system review: five Important findings accepted (unconfirmed cancellation/pause termination, evidence-only output artifact publication, question/checkpoint/evidence prompt context, proposed revision visibility, repository access mappings). Two minors accepted (smoke prose inequality, empty rejection feedback).
Final fix wave: in progress on gpt-6-astra; one coordinated wave then scoped independent re-review. Declined-to-judge items ruled external: real OneDrive/auth/proxy/CLI confinement/company providers/Windows and explicitly out-of-scope multi-host/parallel branches/additional runtimes/public UI. Keep these deployment limits in final handoff.

Final fix wave: implementation frozen; controller independently ran275 passed/1 real-smoke skipped and full build exit0. Scoped final re-review in progress.
Ruling: repository-capable roles use exact selected source/connection sets with separate isolated executionProfile entries where needed; no future random worktree paths are pre-attested. This preserves per-assignment least privilege, at cost of configuring a profile entry for each supported repository combination.
Ruling: unconfirmed process termination preserves blocked pause/cancel intent; an explicit operator exit/effect reconciliation settles the original intent. This avoids false terminal state, at cost of manual resolution when exit cannot be proved.

Final scoped re-review: six findings addressed; Important1 residual confirmed by leader-dead/child-live reproduction.
Ruling: root PID absence cannot establish process-tree exit. Apply the smallest conservative recovery correction and named regression, rather than deliver a known false-cancellation path. This is a bounded correction to the existing final wave, not renewed broad review; uncertain recovery may now require more manual reconciliation. All other final review findings remain closed.

Final scoped review: PASS — all seven findings closed after residual recovery confirmation; no introduced Important finding.
Final controller verification: npm test -- --run at22:20:22,278 passed/1 skipped (279),27 passed files/1 skipped,exit0; npm run build UI+server exit0. Real smoke skipped, no company/model work. Browser1440x900/390x844 verified with isolated fake preview, preview stopped and viewport restored.
Implementation complete for approved local scope. Existing non-Git workspace retained in place; no branch/merge/push or deployment. Review snapshots retained because there is no Git history. External host/OneDrive/auth/profile/provider verification remains explicitly unperformed.
